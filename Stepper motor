ORG 0000H
MOV P1, #00H

LOOP:
MOV P1, #01H
ACALL DELAY

MOV P1, #02H
ACALL DELAY

MOV P1, #04H
ACALL DELAY

MOV P1, #08H
ACALL DELAY

SJMP LOOP

DELAY:
MOV R2, #200
D1: MOV R1, #255
D2: DJNZ R1, D2
DJNZ R2, D1
RET

END
